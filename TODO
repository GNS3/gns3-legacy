TODO LIST

GUI FEATURES:

 * Multilink drawing between 2 routers.
 * Snapshot button.

CORE FEATURES:

 * Add a host symbol which directly use a NIO generic ethernet for its interfaces.
 * Simulated host support (based on LWIP).
 * Add GUI options for filters (monitor etc ...).
 * A standalone server to automatically configure GNS3 clients in order to use remote hypervisors (intended for learning environments, schools/universities, where IOS images cannot be distributed) 
 * NIO multicast support.
 * Show NIO activity.
 * Dynamips QinQ integration.



